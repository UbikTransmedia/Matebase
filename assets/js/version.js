/* ===================================================================
   Matebase · version.js
   LA VERSION DEL CURSO, en un solo sitio. La muestra el pie del indice,
   en la esquina inferior izquierda, y tests.html comprueba su formato.

   Esquema MAYOR.MENOR.PARCHE:
     MAYOR   cambios que reorganizan el temario u obligan a reiniciar
             el progreso guardado;
     MENOR   temas, bloques o herramientas nuevas;
     PARCHE  correcciones, sin contenido nuevo.

   No depende de nada. Se carga el primero.
   =================================================================== */
(function (global) {
  'use strict';

  global.MATEBASE_VERSION = '1.8.0';
})(window);
